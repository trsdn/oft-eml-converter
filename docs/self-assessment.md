# Self-assessment

Per-criterion notes behind [`.github/conformance.yml`](../.github/conformance.yml).

- Standard: [Repository Quality Standard v1.5.1](https://github.com/trsdn/.github/blob/v1.5.1/docs/repository-quality-standard.md)
- Assessed on: 2026-08-30
- State: **Needs work**

The record is the machine-readable result; this document holds the reasoning. Where a criterion is `na`, the rationale is here rather than implied.

## Profiles claimed

| Profile | Applies | Why |
|---|---|---|
| Baseline | yes | Active repository |
| Public | yes | Publicly visible |
| Software | yes | Executes application code |
| Deployable | **no** | Run locally from a checkout; nothing is deployed to a server, container, or cloud environment |
| Package | **no** | Distributed as source. No package is published to PyPI, and releases carry no built artifact — only tags and notes |
| Documentation | **no** | The product is software; `docs/` supports it rather than being the product |
| Published Site | **no** | No site. Every reader is working inside the repository |
| Product Identity | yes | Users run the CLI and GUI directly, so the running product is a surface |
| Agent Readiness | yes | Required in full by the Software profile |
| Language | yes | |
| Accessibility | yes | Ships a CLI and a GUI |
| Data Protection | yes | Processes user files |
| Archived | **no** | Actively maintained |

## Baseline

| ID | Result | Note |
|---|---|---|
| B01 | pass | Name and description state the purpose |
| B02 | pass | README covers purpose, audience, status, setup, and outward links |
| B03 | pass | MIT `LICENSE`, with the `extract-msg` GPL-3.0 boundary stated explicitly |
| B04 | pass | `venv/`, `converter_config.json`, and build output ignored; all maintained source tracked |
| B05 | pass | `python -m pytest tests/ -v`, documented in README and `AGENTS.md` |
| B06 | pass | Ruleset on `main` requiring `Test Suite` and `lint`; Dependabot alerts enabled with no open criticals |
| B07 | pass | `requirements.txt` plus a stated Python range that CI enforces |
| B08 | pass | `CHANGELOG.md` backfilled to v1.0.0, plus GitHub releases |
| B09 | pass | Public, topics set, no homepage because there is no site, not archived |
| B10 | pass | Maintainer and best-effort support status stated in the README |
| B11 | pass | This assessment and the record it backs |
| B12 | pass | `trsdn-standard` topic present |
| B13 | pass | The changelog was duplicated in the README; the README now links to it. Layout detail lives in `AGENTS.md`, architecture in `docs/` |

## Public

| ID | Result | Note |
|---|---|---|
| P01 | pass | MIT, recognized by GitHub |
| P02 | pass | `CONTRIBUTING.md` in-repo; conduct policy inherited from `trsdn/.github` and recognized by GitHub |
| P03 | pass | `SECURITY.md` plus private vulnerability reporting enabled |
| P04 | pass | Issue forms and pull-request template present |
| P05 | pass | Install, usage, examples, compatibility, data handling, security, and support status all covered |
| P06 | pass | Community Standards reports 100% |
| P07 | pass | Description and twelve topics; no homepage is the correct answer here |
| P08 | pass | Required order restored, extra platform badge dropped, and the hand-written Python badge is covered by a drift check in `.github/workflows/standard.yml` |
| P09 | pass | Self-hosted card generated by `.github/workflows/stats.yml` and committed to `.github/stats/` |

## Software

| ID | Result | Note |
|---|---|---|
| S01 | **partial** | Setup is documented and reproducible, but `requirements.txt` uses lower bounds rather than a lockfile, so two clean checkouts can resolve different versions |
| S02 | pass | Suite covers conversion, CLI error paths, headless import, and cross-platform file handling |
| S03 | pass | `flake8`, `black`, `isort`, and `mypy` run in CI |
| S04 | pass | Windows, macOS, and Linux across Python 3.9 to 3.12 — every version the README claims |
| S05 | pass | Secret scanning and push protection enabled |
| S06 | na | No environment-driven configuration surface. The only stored setting is a local last-used directory, which exposes nothing |
| S07 | pass | Errors name the file and the cause. Message headers are printed to the user's own terminal about the user's own file; nothing is transmitted |
| S08 | pass | `.github/dependabot.yml` for pip and Actions, with alerts and security updates enabled |
| S09 | pass | Ruleset requires the existing checks and blocks force pushes and deletion |
| S10 | pass | `docs/ARCHITECTURE.md` and two decision records |

## Deployable, Package, Documentation, Published Site, Archived

`D01`–`D06`, `R01`–`R06`, `T01`–`T05`, `W01`–`W08`, `A01`–`A04` are all `na`. The profile rationale is in the table above. None of these profiles is claimed, so no criterion in them is measured.

## Product Identity

| ID | Result | Note |
|---|---|---|
| I01 | na | No built artifact. The product is source that the user runs |
| I02 | na | As above |
| I03 | na | As above |
| I04 | **fail** | The CLI has no `--version` or `--help` flag, so a running copy cannot report which version it is or where to file a bug. This is the main reason the overall state is not Healthy |
| I05 | na | No artifact to embed an icon in, and no store or installer surface |
| I06 | na | Nothing is embedded, so nothing needs deriving |

## Agent Readiness

| ID | Result | Note |
|---|---|---|
| G01 | pass | `AGENTS.md` at the root |
| G02 | pass | Purpose, layout, setup, run, and validation commands, each verified to run |
| G03 | pass | History rewriting, secrets, releases, settings, generated files, dependencies, and network access all named |
| G04 | pass | `.github/copilot-instructions.md` and `.github/github-app.yml` reference `AGENTS.md` rather than restating it |
| G05 | pass | `python -m pytest tests/ -v` succeeds from a clean checkout |
| G06 | pass | Generated paths listed in `AGENTS.md` under Layout |
| G07 | pass | `Co-authored-by:` trailer plus a stated pull-request and review expectation |
| G08 | pass | `.github/github-app.yml` present and intentional |

## Language And Localization

| ID | Result | Note |
|---|---|---|
| L01 | pass | English declared in the README |
| L02 | pass | All user-facing strings are English |
| L03 | pass | English-only declared |
| L04 | na | No string catalogs |
| L05 | na | No locale-sensitive formatting. Message dates are passed through as the source file recorded them |
| L06 | na | No translations |
| L07 | pass | Repository, docs, comments, identifiers, commits, and issues are English |

## Accessibility

| ID | Result | Note |
|---|---|---|
| X01 | **unknown** | Keyboard operability of the Tkinter GUI has not been verified. Claiming a pass without testing would be the failure `X05` exists to prevent |
| X02 | **unknown** | Accessible names and roles come from Tkinter defaults and have not been inspected |
| X03 | **unknown** | Contrast and text sizing behaviour under platform settings not checked |
| X04 | pass | CLI output is plain ASCII with no colour and no Unicode decoration, asserted by the Windows console encoding tests |
| X05 | pass | The README states the GUI has not been audited and points to the CLI as the accessible path |

## Data Protection And Privacy

| ID | Result | Note |
|---|---|---|
| Y01 | pass | README states nothing is collected and nothing is transmitted |
| Y02 | pass | The only destination is PyPI during install; runtime has none |
| Y03 | na | No telemetry, analytics, or crash reporting exists to disable |
| Y04 | pass | `converter_config.json` named, its location and contents described, and removal explained |
| Y05 | na | No third-party service or AI provider receives user content |
| Y06 | na | Nothing outlives a session except the output files the user asked for |

## Outstanding work

1. `I04` — add `--version` and `--help` to the CLI.
2. `S01` — decide between a lockfile and accepting resolver drift, and record the choice.
3. `X01`–`X03` — verify the GUI against a screen reader and keyboard-only operation, then replace `unknown` with a real result.
